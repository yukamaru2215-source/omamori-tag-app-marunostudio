-- ============================================
-- 備考欄（自由記載） マイグレーション
-- Supabase SQL Editor で実行してください
-- ============================================

-- 出生体重・出生身長・出産週数など、既存の項目に当てはまらない情報を
-- 自由記載できる備考欄。公開/鍵付きは field_visibility.note で管理する
-- （キー未設定の既存レコードはコード側のデフォルト値で "locked" 扱いになる）。
ALTER TABLE children ADD COLUMN IF NOT EXISTS note TEXT;
